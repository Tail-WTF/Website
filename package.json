{
  "name": "tail-wtf-website",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "pnpm": {
    "overrides": {
      "cookie": ">=0.7.0"
    }
  },
  "scripts": {
    "dev": "pnpm --dir frontend build && pnpm --dir api exec wrangler dev",
    "dev:ui": "pnpm --dir frontend dev",
    "build": "pnpm --dir frontend build && pnpm --dir api run build:rules",
    "check": "pnpm --dir api run check && pnpm --dir frontend run check",
    "test": "pnpm --dir api test",
    "test:e2e": "pnpm --dir frontend run test:e2e",
    "deploy": "pnpm run build && pnpm --dir api run deploy"
  }
}
